import ButtonRemove from './ButtonRemove'

export default {
  title: 'Components/ButtonRemove',
  component: ButtonRemove,
}

const Template = (args) => <ButtonRemove {...args} />

export const Simple = Template.bind({})
Simple.args = {}
